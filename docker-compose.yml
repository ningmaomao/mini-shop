services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      SECRET_KEY: change-me-before-production
      DATABASE_URL: sqlite:////app/instance/mini_shop.db
    volumes:
      - mini_shop_data:/app/instance

volumes:
  mini_shop_data:
